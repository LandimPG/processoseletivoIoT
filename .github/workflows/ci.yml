name: Wokwi CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run Wokwi Simulation
      uses: wokwi/wokwi-ci-action@v1
      env:
        # Agora sim, puxando o nome EXATO que está no seu GitHub!
        WOKWI_CLI_TOKEN: ${{ secrets.TOKEN }}
      with:
        expect_text: 'Sistema Iniciado'
        timeout: 15000